fun add(a,b) {
    print a + b;
}

add(1, 2);
print add;

fun sayHi(first, last) {
  print "Hi, " + first + " " + last + "!";
}

sayHi("Dear", "Reader");

fun mult(a, b) {
    return a * b;
}

print mult(2, 3);
